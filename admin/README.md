# SNA Admin Frontend

Administration dashboard built with React and Vite.

```powershell
Copy-Item .env.example .env
npm install
npm run dev
```

The local dashboard runs at `http://localhost:5174/sna/admin/` and expects the
API at `http://localhost:5000`. The production base path is `/sna/admin/`.
