# SNA Customer Frontend

Customer-facing SNA e-commerce application built with React and Vite.

```powershell
Copy-Item .env.example .env
npm install
npm run dev
```

The local site runs at `http://localhost:5173/` and expects the API at
`http://localhost:5000/api/v1`. Run `npm run build` to create `dist/`.
